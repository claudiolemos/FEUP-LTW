let sortHot = document.querySelector('#sort li[name=hot]');
let sortNew = document.querySelector('#sort li[name=new]');
let sortTop = document.querySelector('#sort li[name=top]');

sortHot.addEventListener('click', function(event) {
  event.preventDefault();
});

sortNew.addEventListener('click', function(event) {
  event.preventDefault();
});

sortTop.addEventListener('click', function(event) {
  event.preventDefault();
});
